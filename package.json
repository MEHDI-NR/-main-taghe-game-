{
  "name": "moto-game",
  "version": "0.9.4",
  "private": true,
  "type": "module",
  "description": "Static, modular motorcycle game — GitHub Pages frontend + Supabase backend. Zero runtime dependencies.",
  "scripts": {
    "dev": "python3 -m http.server 8080 --bind 0.0.0.0",
    "check:size": "bash tools/check-size.sh",
    "check:secrets": "bash tools/check-secrets.sh",
    "check": "npm run check:secrets && npm run check:size",
    "build": "node tools/build-protected.mjs",
    "build:single": "node tools/build-single.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright": "^1.62.1"
  }
}
